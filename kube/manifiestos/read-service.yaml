apiVersion: v1
kind: Service
metadata:
  name: read-service
  labels:
    app: read-service
spec:
  type: NodePort
  ports:
  - port: 8081
    targetPort: 8081
    nodePort: 30081
    protocol: TCP
    name: http
  selector:
    app: read-service