server:
  port: 8082

spring:
  application:
    name: redis-service
  redis:
    host: ${SPRING_REDIS_HOST:localhost}
    port: 6379
    timeout: 2000ms

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always